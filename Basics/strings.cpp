#include <iostream>
#include <string>
using namespace std;

int main() {
    
    string str = "This is boogeyman"; // Declare a string variable
    cout << str << endl; // Output the string to standard output

    str = "This is not boogeyman"; // Assign a new value to the string variable
    cout << str << endl; // Output the new string to standard output

    return 0;
}